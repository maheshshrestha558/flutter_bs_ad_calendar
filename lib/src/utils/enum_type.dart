// The type  of calendar to display [AD] or [BS]
enum CalendarType {
  /// English Calendar [AD].
  ad,

  /// Nepali Calendar [BS].
  bs,
}

// The type of widget to display in the view [Day/Month] or [Year]
enum DisplayType {
  // Show month.
  month,
  // Show year.
  year,
}
